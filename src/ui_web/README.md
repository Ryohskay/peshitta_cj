# ui

To run the code:

- `cd ../../`
- `poetry run python -m src.ui_web.app`

## When developing

don't forget to install and start the tailwindcss server:

- `yarn install`
- `yarn dlx @tailwindcss/cli -i ./app.css -o ./static/output.css --watch`
